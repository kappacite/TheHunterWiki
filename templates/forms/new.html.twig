{% extends '/base.html.twig' %}

{% block body %}
    <h1>{{ create_msg }}</h1>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Enregistrer</button>
    {{ form_end(form) }}
{% endblock %}